name: Section Purger
type: module
description: 'Purger for clearing cache on the Section Platform'
package: "Purge - reverse proxies & CDNs"
dependencies:
  - purge:purge
  - purge:purge_tokens
  - key:key

core_version_requirement: ^8.9 || ^9 || ^10
